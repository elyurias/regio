//en app usamos route y definimos el controll
//aqui definimos las rutas

angular.module("regio").controller(
    "MainController",[
        '$scope','$resource','UserData',
        function(s,r,u){

        }
    ]
).controller('LoginController',[
    '$scope','$resource','UserData',function(s,r,u){
        
    }
]);